{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/quantumfate/quickshell/schemas/theme.schema.json",
  "title": "theme.json",
  "description": "The desk's appearance contract. Read by the Quickshell Theme singleton, by the Hyprland opacity rules, and by ,theme.sh apply \u2014 three readers, so a shape change needs a check that is not any one of them.",
  "classification": "declaration",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "palette",
    "mode",
    "day",
    "night",
    "scale",
    "transparency"
  ],
  "properties": {
    "palette": {
      "description": "The active palette. Must name a table in Theme.qml.",
      "type": "string",
      "enum": [
        "latte",
        "frappe",
        "macchiato",
        "mocha"
      ]
    },
    "mode": {
      "description": "auto follows the sun timer; manual pins `palette` until it is handed back.",
      "type": "string",
      "enum": [
        "auto",
        "manual"
      ]
    },
    "day": {
      "type": "string",
      "enum": [
        "latte",
        "frappe",
        "macchiato",
        "mocha"
      ]
    },
    "night": {
      "type": "string",
      "enum": [
        "latte",
        "frappe",
        "macchiato",
        "mocha"
      ]
    },
    "scale": {
      "description": "UI scale. Bounds match the IPC clamp, so the file cannot hold a value the shell would refuse.",
      "type": "number",
      "minimum": 0.8,
      "maximum": 2.5
    },
    "wallpaper": {
      "description": "Absolute path, or \"\" to let the palette decide.",
      "type": "string"
    },
    "transparency": {
      "description": "Global window-transparency dial. 0 is fully opaque (presentation mode); 1 gives each window role the transparency it was designed with.",
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "wallpapers": {
      "description": "Wallpaper per palette. A palette absent here falls back to `wallpaper`, then to <palette>.jpg in the wallpaper directory.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}
